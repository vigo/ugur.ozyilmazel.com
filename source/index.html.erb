---
title: Anasayfa
blog: turkish
cover: "C16.jpg"
layout: :"tr/layout-common"
---

<%
I18n.locale = :tr
@cover_title = "Uğur “vigo” Özyılmazel"
@cover_subtitle = "Bilgisayar Programcısı"
%>

<section class="section main">
    <div class="container">
        
        <div class="columns is-multiline is-variable is-8">
            <div class="column is-9-desktop is-full-tablet">

                <%= partial 'partials/list-blog-posts', locals: {
                    articles: page_articles,
                    which_blog: current_page.metadata[:page][:blog],
                    post_amount: 10,
                } %>

                <div class="content">
                    <p>Toplam <strong><%= page_articles.count %></strong> makale bulunuyor. Tüm blog arişivi için <a href="<%= blog(current_page.metadata[:page][:blog]).options.prefix %>">tıklayın</a>.</p>
                </div>
            </div>
            <div class="column is-3-desktop is-full-tablet">
                <div class="content">
                    <%= partial 'partials/about_me', locals: {
                        locale: I18n.locale,
                    } %>
                    <%= partial 'partials/github_repos', locals: {
                        locale: I18n.locale,
                    } %>
                </div>
            </div>
        </div>

    </div>
</section>
