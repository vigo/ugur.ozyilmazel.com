---
title: Home
blog: english
cover: "C16.jpg"
layout: :"en/layout-common"
---

<%
I18n.locale = :en
@cover_title = "Uğur “vigo” Özyılmazel"
@cover_subtitle = "Computer Programmer"
%>

<section class="section main">
    <div class="container">
        
        <div class="columns is-multiline is-variable is-8">
            <div class="column is-9-desktop is-full-tablet">

                <%= partial 'partials/list-blog-posts', locals: {
                    articles: page_articles,
                    which_blog: current_page.metadata[:page][:blog],
                    post_amount: 3,
                } %>

                <div class="content">
                    <p>There are <strong><%= page_articles.count %></strong> blog posts available. <a href="<%= blog(current_page.metadata[:page][:blog]).options.prefix %>">Click</a> for all blog archive.</p>
                </div>
            </div>
            <div class="column is-3-desktop is-full-tablet">
                <div class="content">
                    <%= partial 'partials/posts_by_year', locals: {
                          locale: I18n.locale,
                          articles: blog.articles,
                    } %>
                </div>
            </div>
        </div>

    </div>
</section>
